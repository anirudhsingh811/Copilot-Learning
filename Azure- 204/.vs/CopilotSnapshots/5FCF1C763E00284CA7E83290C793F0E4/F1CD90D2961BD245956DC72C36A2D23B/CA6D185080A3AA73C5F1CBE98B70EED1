using Microsoft.Extensions.Configuration;
using Microsoft.Extensions.DependencyInjection;
using Microsoft.Extensions.Hosting;
using Microsoft.Extensions.Logging;

namespace AZ204.Monitoring;

class Program
{
    static async Task Main(string[] args)
    {
        var host = Host.CreateDefaultBuilder(args)
            .ConfigureAppConfiguration((context, config) =>
            {
                config.AddJsonFile("appsettings.json", optional: true);
                config.AddEnvironmentVariables();
            })
            .ConfigureServices((context, services) =>
            {
                services.AddLogging(builder =>
                {
                    builder.AddConsole();
                    builder.AddDebug();
                });
            })
            .Build();

        var logger = host.Services.GetRequiredService<ILogger<Program>>();

        Console.WriteLine("???????????????????????????????????????????????????????????????");
        Console.WriteLine("?   AZ-204 Module 4: Monitor, Troubleshoot, and Optimize      ?");
        Console.WriteLine("?   Interactive Learning Environment                           ?");
        Console.WriteLine("???????????????????????????????????????????????????????????????");
        Console.WriteLine();
        Console.WriteLine("?? Coming Soon!");
        Console.WriteLine();
        Console.WriteLine("This module will cover:");
        Console.WriteLine("  • Azure Cache for Redis (cache-aside pattern)");
        Console.WriteLine("  • Azure CDN (Content Delivery Network)");
        Console.WriteLine("  • Application Insights (custom telemetry, KQL)");
        Console.WriteLine("  • Transient fault handling (retry policies)");
        Console.WriteLine("  • Performance optimization");
        Console.WriteLine();
        Console.WriteLine("?? Exam Weight: 15-20%");
        Console.WriteLine();
        Console.WriteLine("Press any key to exit...");
        Console.ReadKey();

        await Task.CompletedTask;
    }
}
